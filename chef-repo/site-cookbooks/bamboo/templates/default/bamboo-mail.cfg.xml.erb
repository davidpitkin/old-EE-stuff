<mail-servers>
  <smtp-server>
    <name><%= @mail['name'] %></name>
    <username><%= @mail['username'] %></username>
    <password><%= @mail['password'] %></password>
    <defaultFrom><%= @mail['defaultFrom'] %></defaultFrom>
    <prefix><%= @mail['prefix'] %></prefix>
    <hostname><%= @mail['hostname'] %></hostname>
    <sessionServer><%= @mail['sessionServer'] %></sessionServer>
    <port><%= @mail['port'] %></port>
    <tlsRequired><%= @mail['tlsRequired'] %></tlsRequired>
  </smtp-server>
</mail-servers>
